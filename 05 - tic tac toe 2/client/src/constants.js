const fieldUrl = 'http://localhost:2000/getFIeld';
const moveUrl = 'http://localhost:2000/move';

const winnerUrl = 'http://localhost:2000/winner';
const resetUrl = 'http://localhost:2000/reset'

module.exports = {
  fieldUrl,
  moveUrl,
  winnerUrl,
  resetUrl,
} 